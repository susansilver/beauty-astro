---
const {prevUrl, nextUrl} = Astro.props
---

<nav class="text-2xl p-3" aria-label="Blog pages">
    {
        prevUrl && (
            <a href={prevUrl}>Previous</a>
        )
    }
{
    nextUrl && (
        <a href={nextUrl}>Next</a>
    )
}
</nav>